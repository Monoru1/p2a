export function Timeline({items}:{items:Array<{title:string;detail:string}>}){return <ol className="relative border-l border-p2a-line pl-7">{items.map((item,index)=><li className="relative pb-10 last:pb-0" key={item.title}><span className="absolute -left-[38px] top-0 grid h-5 w-5 place-items-center rounded-full border-4 border-p2a-canvas bg-p2a-orange text-[9px] font-black text-p2a-ink">{index+1}</span><h3 className="font-extrabold tracking-[-.02em]">{item.title}</h3><p className="mt-2 max-w-xl text-sm leading-6 text-p2a-muted">{item.detail}</p></li>)}</ol>}
