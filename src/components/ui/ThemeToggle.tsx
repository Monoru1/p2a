import { Moon, Sun } from 'lucide-react';
import { useEffect, useState } from 'react';
export function ThemeToggle(){const [dark,setDark]=useState(false);useEffect(()=>{const saved=localStorage.getItem('p2a-theme');const value=saved? saved==='dark':window.matchMedia('(prefers-color-scheme: dark)').matches;setDark(value);document.documentElement.dataset.theme=value?'dark':'light';},[]);const toggle=()=>{const next=!dark;setDark(next);document.documentElement.dataset.theme=next?'dark':'light';localStorage.setItem('p2a-theme',next?'dark':'light')};return <button onClick={toggle} aria-label={dark?'Activer le mode clair':'Activer le mode sombre'} className="rounded-full p-2 text-p2a-muted hover:bg-p2a-violet-soft hover:text-p2a-violet">{dark?<Sun size={19}/>:<Moon size={19}/>}</button>}
