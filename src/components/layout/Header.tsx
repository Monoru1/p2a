import { Menu, X } from 'lucide-react';
import { useState } from 'react';
import { Link, NavLink } from 'react-router-dom';
import { Button } from '../ui/Button';
import { ThemeToggle } from '../ui/ThemeToggle';

const links = [{ to: '/entreprise', label: 'L’entreprise' }, { to: '/formations', label: 'Formations' }, { to: '/prestations', label: 'Prestations' }, { to: '/financements', label: 'Financements' }, { to: '/qualiopi', label: 'Qualiopi' }];
export function Header() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-50 border-b border-p2a-line/80 bg-p2a-canvas/90 backdrop-blur-xl"><div className="mx-auto flex h-[76px] max-w-7xl items-center justify-between px-5 lg:px-8"><Link to="/" className="flex items-center gap-3" aria-label="P2A Formation — Accueil"><img src="/p2a-logo.png" className="h-11 w-11 object-contain" alt=""/><span className="text-base font-extrabold tracking-[-0.04em]">P2A<span className="text-p2a-orange">Formation</span></span></Link><nav className="hidden items-center gap-6 lg:flex">{links.map(l => <NavLink className={({isActive}) => `text-sm font-bold ${isActive ? 'text-p2a-violet' : 'text-p2a-muted hover:text-p2a-ink'}`} to={l.to} key={l.to}>{l.label}</NavLink>)}</nav><div className="hidden items-center gap-2 lg:flex"><ThemeToggle/><Button to="/contact">Parler de votre projet</Button></div><div className="flex items-center gap-1 lg:hidden"><ThemeToggle/><button onClick={() => setOpen(!open)} aria-expanded={open} className="rounded-full p-2 text-p2a-ink">{open ? <X /> : <Menu />}</button></div></div>{open && <nav className="border-t border-p2a-line bg-p2a-canvas px-5 py-5 lg:hidden">{links.map(l => <NavLink onClick={() => setOpen(false)} className="block rounded-xl px-3 py-3 font-bold hover:bg-p2a-violet-soft" to={l.to} key={l.to}>{l.label}</NavLink>)}<Button to="/contact" className="mt-3 w-full">Parler de votre projet</Button></nav>}</header>; }
